# 🚀 دليل البدء السريع - Data Manager

## التشغيل السريع (3 خطوات فقط!)

### 1️⃣ تثبيت الحزم
```bash
npm install --no-bin-links
```

### 2️⃣ تشغيل السيرفر
```bash
npm start
```
أو استخدم:
```bash
./start.sh
```

### 3️⃣ افتح المتصفح
افتح: `http://localhost:3000`

---

## 🔑 API Key

**مهم جداً:** قبل الاستخدام، افتح ملف `.env` وغير `API_KEY`:

```env
API_KEY=your-secure-api-key-here
```

ثم استخدم نفس المفتاح في الواجهة!

---

## 📱 استخدام الواجهة

1. افتح `http://localhost:3000`
2. أدخل API Key في الحقل الأول
3. اضغط "Save Key"
4. ابدأ في إضافة البيانات!

---

## 🧪 اختبار النظام

```bash
# في terminal منفصل (السيرفر يجب أن يكون شغال)
npm test
```

يجب أن ترى: `🎉 All tests passed!`

---

## 📊 استخدام API

### مثال بسيط:

```bash
# إضافة بيانات
curl -X POST http://localhost:3000/api/data \
  -H "Content-Type: application/json" \
  -H "X-API-Key: your-api-key" \
  -d '{"name":"اختبار","value":"مرحبا"}'

# عرض البيانات
curl http://localhost:3000/api/data \
  -H "X-API-Key: your-api-key"
```

---

## 🛑 إيقاف السيرفر

اضغط `Ctrl + C` في terminal السيرفر

---

## 📁 الملفات المهمة

- `server.js` - السيرفر الرئيسي
- `database.js` - قاعدة البيانات
- `routes/api.js` - API endpoints
- `public/` - الواجهة (HTML, CSS, JS)
- `.env` - الإعدادات (API Key هنا!)
- `data.db` - قاعدة البيانات (تُنشأ تلقائياً)

---

## ⚡ نصائح سريعة

✅ **احفظ API Key** - لا تشاركه مع أحد!
✅ **نسخ احتياطي** - احفظ نسخة من `data.db`
✅ **المنفذ مشغول؟** - غير `PORT` في `.env`
✅ **للتطوير** - استخدم `npm run dev` للـ auto-reload

---

## 🆘 مشاكل شائعة

### السيرفر لا يعمل؟
```bash
# تأكد من تثبيت الحزم
npm install --no-bin-links

# تأكد من عدم استخدام المنفذ
lsof -i :3000
```

### API Key خطأ؟
- تأكد من نفس المفتاح في `.env` والواجهة
- تأكد من الضغط على "Save Key"

### البيانات لا تُحفظ؟
- تأكد من صلاحيات الكتابة في المجلد
- تحقق من وجود ملف `data.db`

---

## 📖 للمزيد

اقرأ `README.md` للتفاصيل الكاملة والأمثلة المتقدمة!

---

**صُنع بـ ❤️ - جاهز للاستخدام في Termux!**

